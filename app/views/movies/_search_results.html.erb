<ul>
	<% @movies.each do |movie| %>
		<li><%= link_to movie.title, :action => 'show', :id => movie.id %>
		  | <%= link_to "Add", :controller=> 'screenings', :action => 'create', :id => movie.id -%></li>
	<% end %>
</ul>